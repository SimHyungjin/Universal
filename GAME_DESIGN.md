# 게임 기획서 (Game Design Document)

> 페이트 엑스텔라 링크류 **무쌍 액션** 게임. 본 문서는 흩어진 설계 결정을 하나로 묶은 살아있는 기획서다.
> 상태 표기: ✅ 구현됨 · 🔶 부분/슬라이스 · ⬜ 미구현 · ⏸ 보류(나중에)

---

## 1. 비전 한 줄

**"적을 찢어 디펜더를 분산시켜 각개격파하고, 본진을 함락하는 무쌍."**

정해진 거점 순서를 따라가는 공략이 아니라, 플레이어가 **적 구조를 능동적으로 해체**하는 게 차별점이다. 미니맵은 숫자 시뮬이 아니라 "다음 강습 목표를 고르는 지도"다.

## 2. 핵심 판타지 / 경험 축

- **군중 학살(무쌍)** — 수백 잡몹을 쓸어담는 카타르시스. *이 부분은 충분히 됐다고 판단* — 정밀 타이밍 기믹을 깔지 않는다(슬로모 도배·안 읽힘).
- **전략적 강습 선택** — 적 영역(링크/허브)을 읽고 "어디를 찢을지" 고르는 자유. 이 게임만의 정체성.
- **장수 1:1 결투(깊이)** — 젠존제/명조식 "그 순간". 키 압축 + 반응 루프(예고→퍼펙트닷지→반격→브레이크). 깊이의 타깃.

세 축은 배타적이 아니라 한 리듬으로 엮인다: *강습 대상 선택 → 돌파 학살 → 허브 1:1 → 적 재분산 → 반복 → 본진 결전*.

## 3. 게임 루프 (한 판)  🔶 설계 확정 / 구현 일부

```
시작(침식 보드)
  → 적 영역을 찢어 디펜더 분산
  → 흩어진 디펜더를 1:1 각개격파로 적 엘리트 수를 줄임
  → 본진 진입 = 최종 결전 (살아있는 적 엘리트 전원 소집, 거리차 순차 도착=웨이브)
  → 결전 클리어 = 승리 / 결전 패배 = 게임오버
```

- **본진 직행 방지**: 안 찢고 직행하면 전 엘리트가 소집돼 다대다 지옥 → 미리 각개격파가 곧 결전 준비. "찢기"에 명확한 보상.
- **장수 다수 문제의 해법**: 소집 엘리트의 **순차 도착**이 다대일을 자동으로 "순차 1:1 + 배경 압박"으로 만든다(위협 차등을 따로 안 만들어도 됨).
- ⏸ 게이트 잠금 방식(진입 즉시 노리턴 vs 자발 트리거) — 나중.

## 4. 거시 시스템 (전략 층)

### 4.1 섹터 그래프 · 게이트  ✅
- 절차 생성 다중 섹터(`SectorGenerator`, by-construction으로 항상 연결 보장). 게이트로 인접 연결.
- 섹터 전환 = NavBlob swap(전체 재베이크 없이 ECS Nav 싱글톤 교체). 게이트 통과 = transform Lerp 대쉬(nav 무관).
- 현재 섹터만 실체화, 나머지는 백그라운드 추상.

### 4.2 제로섬 점령 — (A) 통합 점령 압력  ✅
- 진실 단위 = 유닛 수. `AllyTotal + EnemyTotal` 합 고정(제로섬). 점령 게이지 = 비율 파생.
- 잡몹 사망 = 파괴가 아니라 **반대 진영으로 부활** (제로섬 유지).
- **위상(net=링크 영역 크기) + 전투력(aP−eP)을 단일 `enemyward` 압력으로 합성**해 점령을 민다. 규모 불변(병력 400이든 4000이든 같은 비율 속도).
- **플레이어 섹터**: 같은 enemyward를 실체 toggle로 적용하되, 게이지가 점령 기준(`CaptureThreshold`)을 넘으면 적 주입 정지 → 거시 압력이 플레이어의 현장 점령 성과를 못 덮음. "플레이어가 유일한 결정타".
- 튜닝: `SO_SectorBattle_Settings`.

### 4.3 링크 · 허브 · 디펜더 분산  ✅
- 같은 진영 완전 점령 섹터를 게이트로 묶은 **연결 컴포넌트 = 링크**. 그 크기 = `LinkInfluence`.
- **허브 = 절단점**(제거 시 영역을 가장 고르게 쪼개는 섹터). 허브 위치는 hysteresis로 안정화(진동 방지).
- **디펜더(적 엘리트)**가 링크 크기 비례로 각 허브에 분산 집결. 플레이어가 찢으면 영역이 조각나 디펜더가 흩어짐 → 약한 허브 1:1.
- 영토 버프 곡선 불필요: 분배 자체가 "뭉치면 강함 / 찢으면 약함"을 대체.

### 4.4 침식도 시작 보드  ✅
- `erosionStage`(0~9) 다이얼로 시작 진영 분포 산출(`ErosionBootstrap`). 0=전부 아군, 9=한 섹터 빼고 적. 플레이어 시작 섹터는 항상 아군.
- 섹터=용량, 구성(무엇을)=진영별 `SO_Sector_AliveComposition`(실체화 때만 소비).

### 4.5 미니맵  ✅
- 위상 지도. 현재 섹터=실제 위치, 이동=통로 글라이드, 배경=진영별 마커. 강습 목표 선택용.

## 5. 액션 시스템 (전술 층)

### 5.1 무쌍 군중 (ECS 잡몹)  ✅
- Unity DOTS ECS. 수백 체 병렬 처리. 떼 겹침 해소(hard 충돌), 어그로(감지반경+피격 강제).

### 5.2 근접 · 원거리 · 장판 전투  ✅
- `AttackHitEmitter` 단일 히트 판정(GO+ECS, pose 기반). `SO_Attack_Data` 데이터 주도.
- 발사체/장판은 풀 스폰. 히트스톱·흡혈·게이지·컷인 공통.

### 5.3 장수 1:1 반응 루프  🔶
- 구현됨: 공격 중 닷지 캔슬, 퍼펙트 닷지(무효+슬로모+반격창), 키 압축 반격, 브레이크(IsBroken 수식어).
- ⬜ RushTrack 3D 타겟 돌진, 장수 예고 공격 패턴·AI, 패링(선택).

### 5.4 본진 결전  ⬜
- 본진 진입 감지 + 전 엘리트 소집(순차 도착 웨이브) + 승패. 게임 루프의 심장. 순차 도착 속도 = 결전 페이싱 핵심 노브.

## 6. 엘리트(장수)  ✅ (배치/소집은 일부 미구현)

- **"AI가 조종하는 플레이어블 캐릭터"** — 플레이어 전투 스택 100% 재사용 + AI 입력 주입(`Elite_Brain`). 진영은 인스턴스 속성(같은 아키타입도 아군/적 가능).
- **3역할(`BattleRole`)**:
  - **Defender** — 자기 영역 허브 방어, 크기 비례 분산. 플레이어와 같은 섹터면 허브가 아니어도 떠나지 않고 결투.
  - **Vanguard** — 적의 가장 큰 링크 허브를 강습하며 전진.
  - **Duelist** — 적 엘리트만 사냥, 플레이어는 회피.
- 매크로 이동(`Elite_WorldSimulator`): 게이트 트래블, 허브 집결, 이탈 즉시 대쉬(걷다 넉백사 방지).
- ✅ **시작 배치**: 진영 점령 섹터들에 라운드로빈 분산(`SeedStartRoster`) → 시작부터 영역을 지키고, AI가 역할대로 정렬.
- ⬜ **증원**: 없음(N명 고정, 플레이어가 줄여나감). "본진=재생처" 모델은 검토 후 폐기.
- ⏸ **아군 엘리트 역할**: 결전 참전 시 밸런스(아군>적이면 시시) 문제 → 정교한 설계는 나중에 인연/유대 같은 메타 콘텐츠로.

## 7. 현재 상태 요약

| 영역 | 상태 |
|------|------|
| 전투(근접/원거리/장판) | ✅ |
| 섹터·게이트·미니맵 | ✅ |
| 제로섬 점령 (A 압력) + 플레이어 섹터 통합 | ✅ |
| 침식 시작 보드 | ✅ |
| 디펜더 분산 + 매크로 AI(허브 안착·결투·즉시대쉬) | ✅ |
| 장수 반응 루프 | 🔶 슬라이스 |
| 시작 영역 분산 배치 | ✅ |
| **본진 결전** (게임 루프 심장) | ⬜ |
| 승패 → 결과 → 재시작 사이클 | ⬜ |
| 캐릭터 선택 UI | ⬜ |
| 아군 엘리트 역할 / 인연 메타 | ⏸ |

## 8. 다음 작업 우선순위

1. **본진 결전 구현** — 본진 진입 트리거 + 살아있는 적 엘리트 전원 소집(순차 도착) + 승패. 게임 루프를 닫음.
2. 장수 예고 패턴·AI / RushTrack (결투 깊이 강화).
3. 결과·재시작 UI, 캐릭터 선택 UI.

---

*이 문서는 프로젝트 진행에 따라 갱신한다. 세부 구현 결정·이력은 작업 메모리(`.claude/.../memory`)에 남긴다.*
